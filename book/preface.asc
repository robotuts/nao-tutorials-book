== Hello
=== test

kbd:[Ctrl+Alt+Delete]

btn:[Save]

menu:File[New...]


TIP: Asciidoctor supports font-based admonition icons, powered by Font Awesome!

icon:tags[] ruby, asciidoctor

[[_vm_settings]]
==== Запуск виртуальной машины

Для лабораторных работ <<_lab_python>> и <<_lab_python_ext>> была подготовлена конфигурация Vagrant для виртуальной машины с Debian Jessie. Для начала наберите в консоли:

[source,bash]
----
vagrant up
----

Дождитесь загрузки виртуальной машины и подключитесь к ней:

[source,bash]
----
vagrant ssh
----

Первым делом нужно проверить, что SDK корректно установлена в системе. Для этого нужно набрать следующую команду:

[source,bash]
----
python -c "import qi"
----

Если данная операция не выдала никаких ошибок (данная команда вообще не должна ничего выдавать в ответ), то можно приступать к работе.
